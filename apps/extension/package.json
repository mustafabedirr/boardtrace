{
  "name": "@boardtrace/extension",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "vite build && vite build --config vite.content.config.ts && node build/validate-artifact.mjs",
    "build:local": "vite build --mode development && vite build --config vite.content.config.ts --mode development && node build/validate-artifact.mjs",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "chess.js": "^1.4.0"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "eslint": "^9.39.5",
    "prettier": "^3.9.5",
    "typescript": "^5.9.3",
    "vite": "^8.1.4",
    "vitest": "^4.1.10"
  }
}
