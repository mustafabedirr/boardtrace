# Components

The web app provides shadcn Button, Badge, Card, Alert, Tabs, Tooltip, Separator, Dialog, Dropdown Menu, Skeleton, Progress, Switch, Input, Label, Table, Sheet, and Sonner.

BoardTrace primitives are `StatusBadge`, `MoveQualityBadge`, `MetricCard`, `DataValue`, and `ThemeToggle`.
