# Accessibility

Target WCAG 2.2 AA. Color never carries meaning alone: badges include text labels and interactive controls retain visible focus. The theme toggle is keyboard-accessible and hydration-safe. Reduced-motion preferences shorten transitions.
